const OneAnchor = artifacts.require("OneAnchor");
const { BN, expectRevert, time } = require('@openzeppelin/test-helpers');

contract("Swap Tests", async accounts => {

    before(async () => {

    });

    beforeEach(async () =>{

    });

    describe("Swap Tests", () => {

        it("Swap UST for AUST", async () => {

        });

        it("Swap AUST for UST", async () => {

        });

        it("Deposit (Sushiswap + Internal Swap)", async () => {

        });

        it("Withdraw (Sushiswap + Internal Swap)", async () => {

        });

    });

});